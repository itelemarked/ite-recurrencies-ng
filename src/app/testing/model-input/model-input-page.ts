import { Component } from "@angular/core";
import { IonicModule } from '@ionic/angular';
import { Parent } from "./parent";

@Component({
  selector: 'app-model-input-page',
  imports: [
    IonicModule,
    Parent
  ],
  template: `
    <app-parent/>
  `,
  styles: [``]
})
export class ModelInputPage {
  // DEPENDENCIES

  // STATE

  // SELECTORS

  // ACTIONS

  // PRIVATE

}